var i, n;

begin
  n := 15;
  i := 1;
  while i <= n do
    begin
      if i mod 3 = 0 then
        write(i);
      else
        write(n);  /* Properly placed else statement. */
      i := i + 1;
    end;
end.
