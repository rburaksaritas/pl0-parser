var i, n;

begin
  n := 15;
  i := 1;
  while i <= n do
    begin
      if i % 3 = 0 then
        write(i)
      else
        write(0);  /* Correctly placed inside the loop. */
      i := i + 1;
    end;
end.
