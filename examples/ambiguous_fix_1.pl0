var i, n;

begin
  n := 10;
  i := 0;
  while i < n do
    begin
      if i % 2 = 0 then
        write(i)
      else
        write(n);
      i := i + 1;  /* Clearly part of the while loop, not the else. */
    end;
end.
